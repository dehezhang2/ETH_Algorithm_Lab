#include <iostream>
#include <vector>
#include <unordered_map>
constexpr int difference_offset = 12;

using namespace std;
int t, n, k, m; 

void solve(){
    
}

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cin >> t;
    while(t--) solve();
    return 0;
}